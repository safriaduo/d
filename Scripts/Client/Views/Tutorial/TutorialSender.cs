using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using Dawnshard.Network;
using UnityEngine;

public class TutorialSender : MonoBehaviour
{

    /*private IEnumerator Start()
    {
        yield return new WaitUntil(() => GameController.Instance != null && GameController.Instance.Socket != null);

        int tutorial = PlayerPrefs.GetInt(Constants.TutorialStepPrefs, 1);

        if (tutorial == 1)
            StartFirstTutorial();

    }

    private async void StartFirstTutorial()
    {
        //Search match. Match found. Match join. Match Start.
        try
        {
            await GameController.Instance.StartTutorialMatch(1);
        }
        catch (Exception e)
        {
            Debug.LogException(e);
        }
    }*/
}
